---
title: 在 DigitalOcean 上运行
---

[DigitalOcean](https://m.do.co/c/c9ebc1c0928d) 是一家经济实惠的云托管提供商，可以让您托管自己的 Umami 设置。在本设置指南中，我们将安装
[Ubuntu](https://ubuntu.com/)、[PostgreSQL](https://www.postgresql.org/) 或 [MySQL](https://www.mysql.com/) 数据库、
[Nginx](https://www.nginx.com/) Web 服务器、[Node.js](https://nodejs.org/) 和 Umami。
DigitalOcean 也有一个 [NodeJS droplet 构建](https://marketplace.digitalocean.com/apps/nodejs)，其中包含 Node.js、Ubuntu 和 Nginx，可以让您更快入门。

对于个人使用，您可以从每月 5 美元的云服务器开始，
并根据需要扩展。您可以使用此**[链接](https://m.do.co/c/c9ebc1c0928d)**获得前 60 天的 100 美元信用额度。

请注意，这些步骤可以在任何提供 Ubuntu 的云托管提供商上重复。

## 安装 Ubuntu

- [Ubuntu 20.04 初始服务器设置](https://www.digitalocean.com/community/tutorials/initial-server-setup-with-ubuntu-20-04)

## 安装数据库（PostgreSQL 或 MySQL）

- [如何在 Ubuntu 20.04 上安装 PostgreSQL](https://www.digitalocean.com/community/tutorials/how-to-install-postgresql-on-ubuntu-20-04-quickstart)
- [如何在 Ubuntu 20.04 上安装 MySQL](https://www.digitalocean.com/community/tutorials/how-to-install-mysql-on-ubuntu-20-04)

## 安装 Nginx

- [如何在 Ubuntu 20.04 上安装 Nginx](https://www.digitalocean.com/community/tutorials/how-to-install-nginx-on-ubuntu-20-04)

## 安装 Node.js

- [如何在 Ubuntu 20.04 上安装 Node.js](https://www.digitalocean.com/community/tutorials/how-to-install-node-js-on-ubuntu-20-04)

## 安装 Umami

- 请参阅**入门**下的[安装](/docs/install)

## 运行 Umami

您可以简单地运行 `npm start` 来启动 Umami，但强烈建议您使用进程管理器如 [PM2](https://pm2.keymetrics.io/)，它将自动处理重启。

使用 PM2 运行：

```shell
npm install pm2 -g
cd umami
pm2 start npm --name umami -- start
pm2 save
```

## 使用 Nginx 代理

现在 Umami 正在运行，您可以从 Nginx 将请求代理到域名或子域名到 Umami。

以下配置将把所有从 `umami.yourdomain.com` 的请求发送到您的本地 Umami 实例。

```nginx
server {
  server_name umami.yourdomain.com;

  location / {
    proxy_pass http://localhost:3000;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
```

以下配置将允许您在域名的子路径（例：website.com/stats）托管 Umami。
这需要在您的 `.env` 文件中设置环境变量 `BASE_PATH=/stats`。

```nginx
server {
  ...
  location /stats/_next/static/ {
    alias /your_install_location/umami/.next/static/;
    access_log off;
    expires max;
  }
  location /stats {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
  ...
}
```

## 添加 SSL 证书（可选）

- [如何在 Ubuntu 20.04 上使用 Let's Encrypt 保护 Nginx 安全](https://www.digitalocean.com/community/tutorials/how-to-secure-nginx-with-let-s-encrypt-on-ubuntu-20-04)

## 完成

就是这样！您现在在自己的服务器上自托管 Umami 了。
