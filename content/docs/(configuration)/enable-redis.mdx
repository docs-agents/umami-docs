---
title: 启用 Redis
---

Umami 支持将 [Redis](https://redis.io/) 作为缓存层以提高性能。例如，当
您向服务器发送请求时，必须进行网站查找。结果可以缓存在 Redis 中以加快
以后的查找速度。

此外，登录身份验证将由 Redis 处理，而不是使用 JWT 令牌。

## 使用方法

要启用 Redis，请添加作为环境变量名为 `REDIS_URL` 的连接字符串。

```dotenv
REDIS_URL=redis://username:password@your-redis-server:port
```
